import 'package:flutter/material.dart';

class ChennelWidget extends StatelessWidget {
  const ChennelWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}